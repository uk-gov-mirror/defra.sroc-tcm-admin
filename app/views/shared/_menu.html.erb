<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to "Tactical Charging Module", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarRegimeSelectorLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <% if (defined?(@regime)).nil? %>
            Select Regime
          <% else %>
            <%= @regime.name %>
          <% end %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarRegimeSelectorLink">
          <% Regime.all.order(:name).each do |r| %>
            <%= link_to r.name, regime_path(r), class: "dropdown-item" %>
          <% end %>
        </div>
      </li>
      <li class="nav-item active">
        <%= link_to "Regimes", regimes_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Permits</a>
      </li>
      <li class="nav-item">
        <a class="nav-link disabled" href="#">Transactions</a>
      </li>
    </ul>
  </div>
</nav>
