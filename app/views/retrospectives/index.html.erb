<div class="row">
  <div class="col">
    <p id="notice"><%= notice %></p>
  </div>
</div>
<div class="retrospective">
  <div class="row mb-4">
    <div class="col">
      <h1 id="view-title">Retrospectives to be billed</h1>
    </div>
  </div>
  <%= render partial: "table", locals: { transactions: @transactions } %>
  <div id="summary-dialog"
       data-summary-path="<%= regime_retrospective_summary_index_path(@regime) %>">
  </div>
</div>
