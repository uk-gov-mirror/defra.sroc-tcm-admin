<%= form_with(model: regime, local: true) do |form| %>
  <% if regime.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(regime.errors.count, "error") %> prohibited this regime from being saved:</h2>

      <ul>
      <% regime.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :regime_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
